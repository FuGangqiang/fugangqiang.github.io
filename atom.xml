<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>https://fugangqiang.github.io</id>
  <title>Fu</title>
  <updated>2018-04-18T20:57:27+08:00</updated>
  <subtitle>Simple is Beautiful!</subtitle>
  <icon>/static/favicon.png</icon>
  <logo>/static/logo.png</logo>
  <link rel="alternate" type="text/html" href="https://fugangqiang.github.io" />
  <link rel="self" type="application/atom+xml" href="https://fugangqiang.github.io/atom.xml" />
  <generator uri="https://github.com/FuGangqiang/mdblog.rs">mdblog.rs</generator>
  <entry>
    <id>https://fugangqiang.github.io/posts/kafka/%E7%94%A8python%E6%93%8D%E4%BD%9Ckafka.html</id>
    <title>用python操作kafka</title>
    <updated>2018-04-18T20:48:13+08:00</updated>
    <published>2018-04-18T20:48:13+08:00</published>
    <link href="https://fugangqiang.github.io/posts/kafka/%E7%94%A8python%E6%93%8D%E4%BD%9Ckafka.html"/>
    <summary>[上一篇][] 博文介绍了 kafka 集群的搭建， 本博文介绍如何用 [kafka-python][] 包来链接操作 kafka 集群。...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p><a href="/posts/kafka/%E6%90%AD%E5%BB%BAkafka%E9%9B%86%E7%BE%A4.html">上一篇</a> 博文介绍了 kafka 集群的搭建，
本博文介绍如何用 <a href="https://github.com/dpkp/kafka-python">kafka-python</a> 包来链接操作 kafka 集群。</p>
<h2>安装</h2>
<pre><code>pip install kafka-python
</code></pre>
<h2>创建 kafka consumer</h2>
<pre><code>from kafka import KafkaConsumer

consumer = KafkaConsumer('test', bootstrap_servers='10.0.0.2:9092,10.0.0.3:9092,10.0.0.4:9092')
for msg in consumer:
    print (msg)
</code></pre>
<h2>创建 kafka producer</h2>
<pre><code>from kafka import KafkaProducer

producer = KafkaProducer(bootstrap_servers='10.0.0.2:9092,10.0.0.3:9092,10.0.0.4:9092')
for _ in range(100):
    producer.send('test', b'some_message_bytes')
</code></pre>
<h2>脚本测试</h2>
<p><code>producer.py</code> 文件如下：</p>
<pre><code>import time
from kafka import KafkaProducer

producer = KafkaProducer(bootstrap_servers='10.0.0.2:9092,10.0.0.3:9092,10.0.0.4:9092')

i = 1
while True:
    message = f'message {i}'
    print(f'send message: {message}')
    producer.send('test', message.encode())
    i += 1
    time.sleep(3)
</code></pre>
<p><code>consumer.py</code> 文件如下：</p>
<pre><code>from kafka import KafkaConsumer, TopicPartition, OffsetAndMetadata


consumer = KafkaConsumer(bootstrap_servers='10.0.0.2:9092,10.0.0.3:9092,10.0.0.4:9092',
                         group_id='test_group',
                         auto_offset_reset=&quot;earliest&quot;,
                         enable_auto_commit=False)

tp = TopicPartition(topic='test', partition=0)
consumer.assign([tp])
print('start offset is ', consumer.position(tp))

for msg in consumer:
    print(msg)
    consumer.commit({tp: OffsetAndMetadata(msg.offset+1, &quot;Some metadata&quot;)})
</code></pre>
<p>在两个终端上分别先后运行以下命令测试：</p>
<pre><code>python consumer.py
</code></pre>
<p>和</p>
<pre><code>python producer.py
</code></pre>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/kafka/%E6%90%AD%E5%BB%BAkafka%E9%9B%86%E7%BE%A4.html</id>
    <title>搭建kafka集群</title>
    <updated>2018-04-17T11:21:28+08:00</updated>
    <published>2018-04-17T11:21:28+08:00</published>
    <link href="https://fugangqiang.github.io/posts/kafka/%E6%90%AD%E5%BB%BAkafka%E9%9B%86%E7%BE%A4.html"/>
    <summary>[kafka][] 是一个分布式的基于 push-subscribe 的消息系统，快速、可扩展、可持久化， 可以实时的处理大量数据以满足各种需求场景，具有以下特点：...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p><a href="https://kafka.apache.org">kafka</a> 是一个分布式的基于 push-subscribe 的消息系统，快速、可扩展、可持久化，
可以实时的处理大量数据以满足各种需求场景，具有以下特点：</p>
<ul>
<li>高吞吐量、低延迟：每秒可以处理几十万条消息，它的延迟最低只有几毫秒</li>
<li>可扩展性：集群支持热扩展</li>
<li>持久性、可靠性：消息被持久化到本地磁盘，并且支持数据备份防止数据丢失</li>
<li>容错性：允许集群中节点失败（若副本数量为 n,则允许 n-1 个节点失败）</li>
<li>高并发：支持数千个客户端同时读写</li>
</ul>
<p>本博客讲解 debian9.4 系统下 kafka 集群的搭建。</p>
<h2>集群节点配置</h2>
<ul>
<li><code>10.0.0.2</code>: zookeeper, kafka</li>
<li><code>10.0.0.3</code>: zookeeper, kafka</li>
<li><code>10.0.0.4</code>: zookeeper, kafka</li>
</ul>
<h2>安装 zookeeper</h2>
<p>kafka 使用 zookeeper 来维护集群的状态，因此应该首先搭建一个 zookeeper 集群，
具体步骤请见博文：<a href="/posts/zookeeper/%E6%90%AD%E5%BB%BAzookeeper%E9%9B%86%E7%BE%A4.html">搭建zookeeper集群</a>。</p>
<h2>安装 kafka</h2>
<h3>下载 kafka</h3>
<pre><code>wget http://mirrors.tuna.tsinghua.edu.cn/apache/kafka/1.1.0/kafka_2.12-1.1.0.tgz
</code></pre>
<h3>安装 kafka</h3>
<p>分别在三台节点机器上运行解压命令，解压到 <code>/opt</code> 目录：</p>
<pre><code>tar -xzvf kafka_2.12-1.1.0.tgz -C /opt
</code></pre>
<h2>创建 kafka 用户、组</h2>
<pre><code>groupadd kafka
useradd -g kafka -M -s /usr/sbin/nologin kafka
</code></pre>
<h3>配置 kafka</h3>
<p>创建 kafka 数据目录：</p>
<pre><code>mkdir -p /var/lib/kafka
</code></pre>
<p>配置 <code>/opt/kafka_2.12-1.1.0/config/server.properties</code> 文件(<code>10.0.0.2</code> 节点)如下：</p>
<pre><code>broker.id=2
listeners=PLAINTEXT://10.0.0.2:9092
num.network.threads=3
num.io.threads=8
socket.send.buffer.bytes=102400
socket.receive.buffer.bytes=102400
socket.request.max.bytes=104857600
log.dirs=/var/lib/kafka
num.partitions=1
num.recovery.threads.per.data.dir=1
offsets.topic.replication.factor=1
transaction.state.log.replication.factor=1
transaction.state.log.min.isr=1
log.retention.hours=168
log.segment.bytes=1073741824
log.retention.check.interval.ms=300000
zookeeper.connect=10.0.0.2:2181,10.0.0.3:2181,10.0.0.4:2181/kafka
zookeeper.connection.timeout.ms=6000
group.initial.rebalance.delay.ms=0
</code></pre>
<p>其中 <code>broker.id</code> 同 <code>zookeeper</code> 的 <code>myid</code> 类似，是集群中的唯一标识，在此设为 ip 地址尾数。</p>
<h2>配置 systemd service</h2>
<p>文件 <code>/etc/systemd/system/kafka.service</code> 文件如下：</p>
<pre><code>[Unit]
Description=Kafka
After=zookeeper.service

[Service]
Type=simple
User=kafka
Group=kafka
ExecStart=/opt/kafka_2.12-1.1.0/bin/kafka-server-start.sh /opt/kafka_2.12-1.1.0/config/server.properties
Restart=always

[Install]
WantedBy=multi-user.target
</code></pre>
<p>在每一台机器上运行开启 kafka</p>
<pre><code>systemctl start kafka
</code></pre>
<p>可以将 kafka 设为开机自启动：</p>
<pre><code>systemctl enable kafka
</code></pre>
<h2>测试 kafka 集群</h2>
<h3>创建 topic</h3>
<pre><code>/opt/kafka_2.12-1.1.0/bin/kafka-topics.sh \
    --zookeeper 10.0.0.2:2181,10.0.0.3:2181,10.0.0.4:2181/kafka \
    --create \
    --replication-factor 1 \
    --partitions 1 \
    --topic test
</code></pre>
<h3>查看已经存在的 topic</h3>
<pre><code>/opt/kafka_2.12-1.1.0/bin/kafka-topics.sh \
    --zookeeper 10.0.0.2:2181,10.0.0.3:2181,10.0.0.4:2181/kafka \
    --list
</code></pre>
<h3>接收消息</h3>
<pre><code>/opt/kafka_2.12-1.1.0/bin/kafka-console-consumer.sh \
    --bootstrap-server 10.0.0.2:9092,10.0.0.3:9092,10.0.0.4:9092 \
    --topic test \
    --from-beginning
</code></pre>
<h3>发送消息</h3>
<pre><code>/opt/kafka_2.12-1.1.0/bin/kafka-console-producer.sh \
    --broker-list 10.0.0.2:9092,10.0.0.3:9092,10.0.0.4:9092 \
    --topic test
</code></pre>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/zookeeper/%E7%94%A8python%E6%93%8D%E4%BD%9Czookeeper.html</id>
    <title>用python操作zookeeper</title>
    <updated>2018-04-16T15:40:18+08:00</updated>
    <published>2018-04-16T15:40:18+08:00</published>
    <link href="https://fugangqiang.github.io/posts/zookeeper/%E7%94%A8python%E6%93%8D%E4%BD%9Czookeeper.html"/>
    <summary>[上一篇][] 博文介绍了 zookeeper 集群的搭建， 本博文介绍如何用 [python kazoo][] 包来链接操作 zookeeper 集群。...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p><a href="/posts/zookeeper/%E6%90%AD%E5%BB%BAzookeeper%E9%9B%86%E7%BE%A4.html">上一篇</a> 博文介绍了 zookeeper 集群的搭建，
本博文介绍如何用 <a href="https://github.com/python-zk/kazoo">python kazoo</a> 包来链接操作 zookeeper 集群。</p>
<h2>安装</h2>
<pre><code>pip install kazoo
</code></pre>
<h2>创建 zookeeper client</h2>
<pre><code>from kazoo.client import KazooClient

zk = KazooClient(hosts='10.0.0.2:2181')

zk.start()
# some operations
zk.stop()
</code></pre>
<p>上面的 <code>zk</code> 对象就是 zookeeper client 的一个实例，
所有针对 zookeeper 的操作都是通过 zk 对象操作的。</p>
<h2>zookeeper 命令</h2>
<p><code>zk</code> 对象以下方法与 zookeeper cli client 命令类似：</p>
<ul>
<li>ensure_path</li>
<li>create</li>
<li>get</li>
<li>get_children</li>
<li>set</li>
<li>delete</li>
</ul>
<pre><code>zk.ensure_path(&quot;/my/favorite&quot;)
zk.create(&quot;/my/favorite/node&quot;, b&quot;a value&quot;)

if zk.exists(&quot;/my/favorite&quot;):
    data, stat = zk.get(&quot;/my/favorite&quot;)
    print(f'Version: {stat}, data: {data}')
    children = zk.get_children(&quot;/my/favorite&quot;)
    print(f'Children are now: {children}')

zk.set(&quot;/my/favorite&quot;, b&quot;some data&quot;)
zk.delete(&quot;/my&quot;, recursive=True)
</code></pre>
<p>kazoo 也提供了 <code>ChildrenWatch</code> 和 <code>DataWatch</code> 的装饰器来监控 zookeeper 里面的数据。</p>
<h2>脚本测试</h2>
<p><code>watch.py</code> 文件如下：</p>
<pre><code>from kazoo.client import KazooClient


class ZooKeeper(KazooClient):
    def __init__(self, timeout=15, *args, **kwargs):
        super().__init__(timeout=timeout, *args, **kwargs)
        self.start(timeout)

    def __del__(self):
        self.stop()


zk = ZooKeeper(hosts='10.0.0.2:2181,10.0.0.3:2181,10.0.0.4:2181')
zk.ensure_path('/app/hello')

@zk.ChildrenWatch('/app/hello')
def watch_children(children):
    print(f'Children are now: {children}')


@zk.DataWatch('/app/hello/database')
def watch_node(data, stat):
    print(f'Version: {stat}, data: {data}')


input('press any key to stop:')
</code></pre>
<p><code>modify.py</code> 文件如下：</p>
<pre><code>from kazoo.client import KazooClient


class ZooKeeper(KazooClient):
    def __init__(self, timeout=15, *args, **kwargs):
        super().__init__(timeout=timeout, *args, **kwargs)
        self.start(timeout)

    def __del__(self):
        self.stop()


zk = ZooKeeper(hosts='10.0.0.2:2181,10.0.0.3:2181,10.0.0.4:2181')
zk.ensure_path('/app/hello')

zk.create('/app/hello/database', b'10.0.0.6:5432')
# zk.set('/app/hello/database', b'10.0.0.6:3306')
</code></pre>
<p>在两个终端上分别先后运行以下命令测试：</p>
<pre><code>python watch.py
</code></pre>
<p>和</p>
<pre><code>python modify.py
</code></pre>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/zookeeper/%E6%90%AD%E5%BB%BAzookeeper%E9%9B%86%E7%BE%A4.html</id>
    <title>搭建zookeeper集群</title>
    <updated>2018-04-16T11:06:18+08:00</updated>
    <published>2018-04-16T11:06:18+08:00</published>
    <link href="https://fugangqiang.github.io/posts/zookeeper/%E6%90%AD%E5%BB%BAzookeeper%E9%9B%86%E7%BE%A4.html"/>
    <summary>[zookeeper][] 是一个为分布式应用提供高效、高可用的分布式协调服务， 提供了诸如数据发布/订阅、负载均衡、命名服务、分布式协调/通知和分布式锁等分布式基础服务。...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p><a href="https://zookeeper.apache.org/">zookeeper</a> 是一个为分布式应用提供高效、高可用的分布式协调服务，
提供了诸如数据发布/订阅、负载均衡、命名服务、分布式协调/通知和分布式锁等分布式基础服务。</p>
<p>本博客讲解 debian9.4 系统下 zookeeper 集群的搭建，集群机器 ip 如下：</p>
<ul>
<li><code>10.0.0.2</code></li>
<li><code>10.0.0.3</code></li>
<li><code>10.0.0.4</code></li>
</ul>
<h2>安装包</h2>
<p>分别在集群机器上安装 java 环境包和 zookeeperd 包：</p>
<pre><code>apt install openjdk-8-jre-headless
apt install zookeeperd
</code></pre>
<h2>创建工作目录</h2>
<pre><code>mkdir -p /var/lib/zookeeper
mkdir -p /var/log/zookeeper
</code></pre>
<h2>配置 zookeeper myid</h2>
<p>zookeeper 利用 <code>myid</code> 来唯一标识 zookeeper 节点，从而利用它来辨别集群中的每一个节点，
因此集群中的每一个 zookeeper 节点的 <code>myid</code> 不能重复出现，
我们将 <code>myid</code> 写在 <code>/var/lib/zookeeper/myid</code> 文件中：</p>
<p>在此将三台机器的 ip 尾数作为 zookeeper 节点的 <code>myid</code>，比如 <code>10.0.0.3</code> 节点的 <code>myid</code> 为 <code>3</code>，
因此在 <code>10.0.0.3</code> 机器上的 <code>/var/lib/zookeeper/myid</code> 文件内容为：</p>
<pre><code>3
</code></pre>
<h2>配置 zookeeper zoo.cfg</h2>
<p>每一个节点机器上都需要配置 <code>/etc/zookeeper/conf/zoo.cfg</code> 文件，配置如下：</p>
<pre><code>dataDir=/var/lib/zookeeper
dataLogDir=/var/log/zookeeper
tickTime=2000
clientPort=2181
initLimit=5
syncLimit=2
autopurge.purgeInterval=48
autopurge.snapRetainCount=10

server.2=10.0.0.2:2888:3888
server.3=10.0.0.3:2888:3888
server.4=10.0.0.4:2888:3888
</code></pre>
<h2>配置 systemd service</h2>
<p>zookeeper 的 service 已经被安装在 <code>/lib/systemd/system/zookeeper.service</code> 中，
内容如下：</p>
<pre><code>[Unit]
Description=Coordination service for distributed applications
After=network.target
ConditionPathExists=/var/lib/zookeeper/myid

[Service]
Type=simple
User=zookeeper
SyslogIdentifier=zookeeper
EnvironmentFile=/etc/zookeeper/conf/environment
EnvironmentFile=-/etc/default/zookeeper
ExecStart=/usr/bin/java -cp ${CLASSPATH} $JAVA_OPTS -Dcom.sun.management.jmxremote -Dcom.sun.management.jmxre
SuccessExitStatus=143
# ZooKeeper is &quot;fail-fast&quot;, see https://zookeeper.apache.org/doc/r3.4.8/zookeeperAdmin.html#sc_supervision
Restart=always

[Install]
WantedBy=multi-user.target
</code></pre>
<p>在每一台机器上运行开启 zookeeper：</p>
<pre><code>systemctl start zookeeper
</code></pre>
<p>可以将 zookeeper 设为开机自启动：</p>
<pre><code>systemctl enable zookeeper
</code></pre>
<h2>检测 zookeeper</h2>
<p>随便 ssh 进入一台机器，运行以下命令查看 zookeeper 集群健康情况：</p>
<pre><code>echo mntr | nc 127.0.0.1 2181
</code></pre>
<p>类似四字命令也有：</p>
<ul>
<li>conf</li>
<li>cons</li>
<li>crst</li>
<li>dump</li>
<li>envi</li>
<li>ruok</li>
<li>srst</li>
<li>srvr</li>
<li>stat</li>
<li>wchs</li>
<li>wchc</li>
<li>wchp</li>
</ul>
<p>具体详情请看：<a href="https://zookeeper.apache.org/doc/current/zookeeperAdmin.html">zookeeper Admin 文档</a>。</p>
<h2>测试 zookeeper 命令</h2>
<p>开启 zookeeper cli 客户端：</p>
<pre><code>cd /usr/share/zookeeper
./bin/zkCli.sh -server 127.0.0.1
</code></pre>
<p>进入 cli 环境后就可以测试各种 zookeeper 命令了：</p>
<pre><code>[zk: 127.0.0.1(CONNECTED) 0] help
ZooKeeper -server host:port cmd args
    stat path [watch]
    set path data [version]
    ls path [watch]
    delquota [-n|-b] path
    ls2 path [watch]
    setAcl path acl
    setquota -n|-b val path
    history 
    redo cmdno
    printwatches on|off
    delete path [version]
    sync path
    listquota path
    rmr path
    get path [watch]
    create [-s] [-e] path data acl
    addauth scheme auth
    quit 
    getAcl path
    close 
    connect host:port
</code></pre>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/ops/vagrant%E6%90%AD%E5%BB%BA%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83.html</id>
    <title>vagrant搭建开发环境</title>
    <updated>2018-04-13T12:07:57+08:00</updated>
    <published>2018-04-13T12:07:57+08:00</published>
    <link href="https://fugangqiang.github.io/posts/ops/vagrant%E6%90%AD%E5%BB%BA%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83.html"/>
    <summary>[Vagrant][] 让我们可以通过 ruby 脚本来快速、可重复的在本地搭建虚拟机测试集群， 与 ansible/chef 等组合使用更是方便，是本地统一开发测试的极佳运维工具。...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p><a href="https://www.vagrantup.com/">Vagrant</a> 让我们可以通过 ruby 脚本来快速、可重复的在本地搭建虚拟机测试集群，
与 ansible/chef 等组合使用更是方便，是本地统一开发测试的极佳运维工具。</p>
<p>本文讲解如何制作最初始的 vagrant box。</p>
<h2>安装软件</h2>
<ul>
<li>安装 virtualbox</li>
<li>安装 vagrant</li>
</ul>
<h2>制作虚拟机</h2>
<ul>
<li>下载 debian iso 镜像</li>
<li>利用下载的镜像在 virtualbox 安装一个名为 <code>vmname</code> 的 linux 操作系统</li>
</ul>
<h2>debian 虚拟机配置</h2>
<ul>
<li>在虚拟机中创建 <code>vagrant</code> 用户和用户目录，密码为 <code>vagrant</code></li>
<li>在虚拟机中添加 <code>vagrant</code> 用户公共密钥
<pre><code>curl https://raw.githubusercontent.com/hashicorp/vagrant/master/keys/vagrant.pub &gt; .ssh/authorized_keys
</code></pre>
</li>
<li>基础包安装 <code>apt install ...</code>
<ul>
<li>sudo</li>
<li>gcc</li>
<li>make</li>
<li>perl</li>
<li>openssh-client</li>
<li>openssh-server</li>
<li>linux-headers-amd64</li>
</ul>
</li>
<li>安装 virtualbox guest additions（有可能需要重启）
<ul>
<li>选择虚拟机菜单 <code>Devices</code> -&gt; <code>Insert Guest Additons CD image...</code></li>
<li>挂载 cdrom <code>mount /dev/cdrom /mnt</code></li>
<li>进入 cdrom 目录 <code>cd /mnt</code></li>
<li>安装 virtualbox guest additions <code>./VBoxLinuxAdditions.run</code></li>
</ul>
</li>
<li>将 <code>vagrant</code> 加入 <code>sudo</code> 用户组中，visudo 修改 <code>%sudo ALL=(ALL:ALL) NOPASSWD:ALL</code></li>
</ul>
<h2>导出 box</h2>
<ul>
<li>在本机中执行 <code>vagrant package --base vmname /path/to/debian.box</code></li>
</ul>
<h2>导入 box</h2>
<p><code>vagrant box add debian /path/to/debian.box</code></p>
<h2>创建 box 虚拟机</h2>
<ul>
<li>创建 vagrant 目录 <code>mkdir debian &amp;&amp; cd debian</code></li>
<li>初始化运行环境, <code>vagrant init debian</code></li>
<li>创建虚拟机 <code>vagrant up</code></li>
</ul>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/rust/rust%E4%B9%8Btokio.html</id>
    <title>rust之tokio</title>
    <updated>2018-04-11T15:10:40+08:00</updated>
    <published>2018-04-11T15:10:40+08:00</published>
    <link href="https://fugangqiang.github.io/posts/rust/rust%E4%B9%8Btokio.html"/>
    <summary>随着 tokio 0.1 的发布，rust 语言的异步 io 库逐渐成熟， 打算用几篇博客来描述一下 tokio 框架相关的基础 crate(当然，也是为了学习一下)：...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p>随着 tokio 0.1 的发布，rust 语言的异步 io 库逐渐成熟，
打算用几篇博客来描述一下 tokio 框架相关的基础 crate(当然，也是为了学习一下)：</p>
<ul>
<li><a href="https://github.com/carllerche/mio">mio</a></li>
<li><a href="https://github.com/alexcrichton/futures-rs">futures</a></li>
<li><a href="https://github.com/tokio-rs/tokio">tokio</a></li>
</ul>
<p>本篇主要讲解 <a href="https://github.com/tokio-rs/tokio">tokio</a>。</p>
<p>直接通过 mio 我们也是可以写出异步 IO 程序的，但是 mio 提供的接口太过于底层，
以至于直接用 mio 时需要保存 token 和链接的关系，并且还要在链接就绪后去执行特定的程序，
这些都太过于繁杂，但是这个过程存在一个事件分派模式，
我们可以把这个模式抽象出来，所以 tokio 应运而生。</p>
<p>tokio 基于 mio 和 futures，
通过 mio 来管理 IO 事件，通过 futures 来表达对各种 IO 事件的处理，
这样 tokio 提供了更方便的接口供用户使用。</p>
<p>我们可以通过以下代码示例来说明 tokio 的用法：</p>
<pre><code class="language-rust">extern crate tokio;

use tokio::io;
use tokio::net::TcpListener;
use tokio::prelude::*;

fn main() {
    let addr = &quot;127.0.0.1:12345&quot;.parse().unwrap();
    let tcp = TcpListener::bind(&amp;addr).unwrap();

    let server = tcp.incoming().for_each(|tcp| {
        let (reader, writer) = tcp.split();
        let conn = io::copy(reader, writer)
            .map(|(n, _, _)| {
                println!(&quot;wrote {} bytes&quot;, n)
            })
            .map_err(|err| {
                println!(&quot;IO error {:?}&quot;, err)
            });

        tokio::spawn(conn);
        Ok(())
    })
    .map_err(|err| {
        println!(&quot;server error {:?}&quot;, err);
    });

    tokio::run(server);
}
</code></pre>
<p>上面代码实现的功能与 epoll/mio 篇中实现的功能完全一样，通过对比，
我们可以看出 tokio 写出的代码更具有可读性，
它隐藏了直接通过 mio 接口调用的大量繁杂的代码，
又通过各种自定义的 futures 来定义事件处理程序。</p>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/rust/rust%E4%B9%8Bfutures-rs.html</id>
    <title>rust之futures-rs</title>
    <updated>2018-04-11T15:08:33+08:00</updated>
    <published>2018-04-11T15:08:33+08:00</published>
    <link href="https://fugangqiang.github.io/posts/rust/rust%E4%B9%8Bfutures-rs.html"/>
    <summary>随着 tokio 0.1 的发布，rust 语言的异步 io 库逐渐成熟， 打算用几篇博客来描述一下 tokio 框架相关的基础 crate(当然，也是为了学习一下)：...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p>随着 tokio 0.1 的发布，rust 语言的异步 io 库逐渐成熟，
打算用几篇博客来描述一下 tokio 框架相关的基础 crate(当然，也是为了学习一下)：</p>
<ul>
<li><a href="https://github.com/carllerche/mio">mio</a></li>
<li><a href="https://github.com/alexcrichton/futures-rs">futures</a></li>
<li><a href="https://github.com/tokio-rs/tokio">tokio</a></li>
</ul>
<p>本篇主要讲解 <a href="https://github.com/alexcrichton/futures-rs">futures</a>。</p>
<p>在许多编程语言都有 <code>futures</code>(类似于 <code>promise</code>、<code>delay</code>、<code>deferred</code>) 的概念，
并在此基础上引入轻量级进程或协程的概念。</p>
<h2>为什么需要 futures？</h2>
<p>在异步并发编程中同时需要维护成千上万个链接，这些链接大多处于阻塞状态，
当其中某些链接就绪后（由 mio 提供的接口询问），服务程序需要分别对其进行不同的处理，
处理这些链接的方法大多用两种方式：</p>
<ul>
<li>callbacks</li>
<li>futures</li>
</ul>
<p>虽然 callbacks 方式便于理解，但是当回调的嵌套层次比较深时，程序代码维护和阅读很不方便，
为此引入了 futures 方式来表达程序的执行流程，进而使代码像同步代码一样。</p>
<h2>什么是 futures？</h2>
<p>本质上 <code>future</code> 就是一个值，但是这个值有可能还没有就绪，而就绪是需要某些事件的发生，
其适用于任何事件，我们可以用 <code>future</code> 来表示以下事件：</p>
<ul>
<li>一个 RPC 调用</li>
<li>一个数据库查询</li>
<li>一个计时器</li>
<li>一个非常耗时的任务</li>
<li>从 socket 中读数据</li>
</ul>
<p>不仅如此，我们可以对 <code>future</code> 进行任意的组合，比如：</p>
<ul>
<li>顺序组合(sequential composition)：
<code>f.and_then(|val| some_new_future(val))</code>，
获取 future <code>f</code> 的值，将其传入一个函数进而构造另一个 future</li>
<li>映射组合(Mapping)：
<code>f.map(|val| some_new_value(val))</code>，
获取 future <code>f</code> 的值，将其传入一个函数进而构造另一个值</li>
<li>合并组合(Joining)：
<code>f.join(g)</code>，
使 future <code>f</code> 和 future <code>g</code> 并发执行，当两者都就绪后将两者的值合并为一个元组</li>
<li>选择组合(Selecting)：
<code>f.select(g)</code>，
使 future <code>f</code> 和 future <code>g</code> 并发执行，当其中一个就绪后并选择这个 future 的值返回</li>
</ul>
<p>通过上面的特性我们可以很容易用 <code>futures</code> 来表达下面的执行流：</p>
<pre><code class="language-rust">id_rpc(&amp;my_server).and_then(|id| {
    get_row(id)
}).map(|row| {
    json::encode(row)
}).and_then(|encoded| {
    write_string(my_socket, encoded)
})
</code></pre>
<p>这看起来很像同步代码，但是上面代码最终会被编译成一个状态机，根据当前的状态执行特定程序。</p>
<h2>futures-rs future 种类</h2>
<p>从上面看 future 只是代表一个单值，只代表一个事件，
但是在网络编程中，读取一个 TCP 链接我们通常会获取一个字节流，与一系列读事件相关，
因此我们还需要另一种 future 抽象用来关联多个事件：</p>
<ul>
<li>Stream：读入流，与 incoming events 关联</li>
<li>Sink：写出流，与 outgoing events 关联</li>
</ul>
<p>因此 futures-rs 分别提供了三种 trait：</p>
<ul>
<li>Future</li>
<li>Stream</li>
<li>Sink</li>
</ul>
<h2>trait 定义</h2>
<p>trait <code>Future</code> 的定义与标准库中的 <code>Iterator</code> 很相似：</p>
<pre><code class="language-rust">trait Future {
    // The type of value that the future yields on successful completion.
    type Item;

    // The type of value that the future yields on failure.
    type Error;

    // The only required method, which attempts to complete the future.
    fn poll(&amp;mut self) -&gt; Poll&lt;Self::Item, Self::Error&gt;;

    ...
}

</code></pre>
<p>定义一个 <code>Future</code> 只需定义 <code>Item</code>、<code>Error</code> 类型和 <code>poll</code> 方法就可以了，
其他与 <code>Future</code> 有关的方法都是基于这三个的，已经预先定义了，不需要用户自定义了。</p>
<p><code>Stream</code> 和 <code>Sink</code> 也与 <code>Future</code> 类似：</p>
<pre><code class="language-rust">trait Stream {
    // The type of item yielded each time the stream's event occurs
    type Item;

    // The error type; errors terminate the stream.
    type Error;

    // Try to produce a value.
    fn poll(&amp;mut self) -&gt; Poll&lt;Option&lt;Self::Item&gt;, Self::Error&gt;;

    ...
}

trait Sink {
    // The type of value that the sink accepts.
    type SinkItem;

    // The type of value produced by the sink when an error occurs.
    type SinkError;

    // The analog to `poll`, used for sending and then flushing items.
    fn start_send(&amp;mut self, item: Self::SinkItem) -&gt; StartSend&lt;Self::SinkItem, Self::SinkError&gt;;

    fn poll_complete(&amp;mut self) -&gt; Poll&lt;(), Self::SinkError&gt;;

    ...
}
</code></pre>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/rust/rust%E4%B9%8Bmio.html</id>
    <title>rust之mio</title>
    <updated>2018-04-11T15:02:25+08:00</updated>
    <published>2018-04-11T15:02:25+08:00</published>
    <link href="https://fugangqiang.github.io/posts/rust/rust%E4%B9%8Bmio.html"/>
    <summary>随着 tokio 0.1 的发布，rust 语言的异步 io 库逐渐成熟， 打算用几篇博客来描述一下 tokio 框架相关的基础 crate(当然，也是为了学习一下)：...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p>随着 tokio 0.1 的发布，rust 语言的异步 io 库逐渐成熟，
打算用几篇博客来描述一下 tokio 框架相关的基础 crate(当然，也是为了学习一下)：</p>
<ul>
<li><a href="https://github.com/carllerche/mio">mio</a></li>
<li><a href="https://github.com/alexcrichton/futures-rs">futures</a></li>
<li><a href="https://github.com/tokio-rs/tokio">tokio</a></li>
</ul>
<p>本篇主要讲解 <a href="https://github.com/carllerche/mio">mio</a>。</p>
<p>mio(Metal IO) 是 rust 语言中的异步 io 的基础 crate，tokio 框架就是基于这个 crate 的。
从源码上看 mio 基本就是对 linux epoll(bsd kqueue) 的简单包装，只是披了一层 rust 外衣，
性能与原生相同，接口调用方式与 linux epoll 极为相似，mio 本身实现了可以监控以下几种事件：</p>
<ul>
<li>tcp io</li>
<li>udp io</li>
<li>timer</li>
<li>channel(mpsc)</li>
</ul>
<p>同时，mio 也提供了一种扩展机制，可以很轻松的实现以下类似事件：</p>
<ul>
<li>uds(unix domain socket)</li>
<li>signal</li>
<li>pipe</li>
<li>thread</li>
<li>subprocess</li>
</ul>
<p>当然在看这篇文章之前，你需要了解 linux epoll 的知识，
可以查看博文<a href="/posts/linux/%E5%A6%82%E4%BD%95%E4%BD%BF%E7%94%A8epoll%E7%B3%BB%E7%BB%9F%E8%B0%83%E7%94%A8.html">如何使用epoll系统调用</a>，本文示例与 epoll 示例功能实现完全相同。</p>
<p>我们可以通过以下示例来看一下 mio 的用法：</p>
<pre><code class="language-rust">extern crate mio;
extern crate slab;

use std::net::SocketAddr;
use std::io::{Read, Write, ErrorKind};
use mio::*;
use mio::tcp::TcpListener;

type Slab&lt;T&gt; = slab::Slab&lt;T, Token&gt;;

const SERVER_TOKEN: Token = Token(::std::usize::MAX-1);


fn main() {
    let mut args = ::std::env::args();
    let cmd = args.next().unwrap();
    let port = args.next().expect(&amp;format!(&quot;Usage: {} [port]&quot;, cmd));
    let addr: SocketAddr = format!(&quot;127.0.0.1:{}&quot;, port).parse().expect(&quot;argument format error: port&quot;);
    let server = TcpListener::bind(&amp;addr).expect(&quot;socket binding error&quot;);
    let poll = Poll::new().expect(&quot;poll create error&quot;);
    let mut conns = Slab::with_capacity(1024);
    let mut events = Events::with_capacity(1024);
    let mut buf: [u8; 1024] = [0; 1024];
    let stdout = ::std::io::stdout();

    poll.register(&amp;server, SERVER_TOKEN, Ready::readable(), PollOpt::edge())
        .expect(&quot;poll register error&quot;);
    // the event loop
    loop {
        poll.poll(&amp;mut events, None).expect(&quot;poll error&quot;);
        for event in events.iter() {
            let (token, kind) = (event.token(), event.kind());

            if kind.is_error() || kind.is_hup() || !kind.is_readable() {
                println!(&quot;kind error&quot;);
                if token == SERVER_TOKEN {
                    ::std::process::exit(1);
                }
                conns.remove(token);
            } else if token == SERVER_TOKEN {
                loop {
                    let sock = match server.accept() {
                        Ok((sock, addr)) =&gt; {
                            println!(&quot;Accepted connection: {}&quot;, addr);
                            sock
                        }
                        Err(_) =&gt; break
                    };
                    let new_token = conns.insert(sock).expect(&quot;add connection error&quot;);
                    poll.register(&amp;conns[new_token], new_token, Ready::readable(), PollOpt::edge())
                        .expect(&quot;poll register error&quot;);

                }
            } else {
                let mut need_to_close = false;
                {
                    let ref mut client = conns[token];
                    loop {
                        match client.read(&amp;mut buf) {
                            Ok(n) =&gt; {
                                if n == 0 {
                                    need_to_close = true;
                                    break;
                                } else {
                                    let mut handle = stdout.lock();
                                    handle.write(&amp;buf[..n]).expect(&quot;write error&quot;);
                                    handle.flush().expect(&quot;flush error&quot;);
                                }
                            },
                            Err(e) =&gt; {
                                if e.kind() != ErrorKind::WouldBlock {
                                    need_to_close = true;
                                }
                                break;
                            }
                        }
                    }
                }
                if need_to_close {
                    println!(&quot;Closing connection on token={:?}&quot;, token);
                    conns.remove(token);
                }

            }
        }
    }
}
</code></pre>
<p>我们从源码上面看，代码结构与 linux epoll 大致一样，
有一个显著的不同就是 mio 用了 token(usize 类型) 来绑定 tcp 链接，
这个应该主要是因为在 linux 系统中，每一个 socket 都是一个文件，对应一个文件描述符(usize 类型)，
而 rust 语言标准库里并没有跨平台的文件描述符类型，
而且也为了扩展其他事件类型，
引入 token 机制，统一了事件的索引类型。</p>
<p>示例代码中也引入了 <code>slab</code> crate 用来保存 token 和链接的一一对应关系，
当然，你也可以用 hashmap 来建立索引，不过这个不是很高效。</p>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/rust/%E7%94%A8%20mdblog%20%E6%9E%84%E5%BB%BA%E9%9D%99%E6%80%81%E5%8D%9A%E5%AE%A2.html</id>
    <title>用 mdblog 构建静态博客</title>
    <updated>2018-04-09T10:34:00+08:00</updated>
    <published>2018-04-09T10:34:00+08:00</published>
    <link href="https://fugangqiang.github.io/posts/rust/%E7%94%A8%20mdblog%20%E6%9E%84%E5%BB%BA%E9%9D%99%E6%80%81%E5%8D%9A%E5%AE%A2.html"/>
    <summary>最近刚好有点空闲，就继续完善 [mdblog.rs][] 项目， 再加上 [rust][] 语言生态最近逐渐完善以及 rust 语言的特点， 重构起来真是特别的舒心，也体验到了静态语言的强大优势。...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p>最近刚好有点空闲，就继续完善 <a href="https://github.com/FuGangqiang/mdblog.rs">mdblog.rs</a> 项目，
再加上 <a href="https://www.rust-lang.org/en-US/">rust</a> 语言生态最近逐渐完善以及 rust 语言的特点，
重构起来真是特别的舒心，也体验到了静态语言的强大优势。</p>
<p>经过数个不兼容版本的迭代，<a href="https://github.com/FuGangqiang/mdblog.rs">mdblog.rs</a> 终于有了我想要的所有功能：</p>
<ul>
<li>配置简单</li>
<li>TeX 数学公式</li>
<li>博客 url 为文件路径，而不是日期</li>
<li>博客标题为源文件名</li>
<li>博客索引页面分页</li>
<li>可以设置博客的 base url</li>
<li>可以隐藏某些尚不准备发布的博客</li>
<li>可以自定义博客 theme 样式</li>
<li><code>serve</code> 命令监控源文件变化自动构建博客</li>
<li><code>rss/atom</code> Feed 支持</li>
</ul>
<p>当然也用了许多 rust 的很优秀 crates：</p>
<ul>
<li><code>structopt</code>：替换了原来的 <code>getopts</code></li>
<li><code>failure</code>：替换了原来的 <code>error-chain</code></li>
<li><code>serde_yaml</code>：替换了原来的手动解析博客的 header</li>
<li><code>tempdir</code>：实现了用临时文件夹来存放 <code>serve</code> 命令产生的临时静态文件</li>
<li><code>open</code>：浏览器自动打开 <code>serve</code> 命令产生的博客测试首页</li>
<li><code>hyper</code>：用于 <code>serve</code> 命令产生的测试后台 http 服务</li>
<li><code>shellexpand</code>：自动对博客配置某些目录进行变量或特殊符号的替换功能</li>
<li><code>config</code>：替换了原来的 <code>toml</code>，实现了配置项的分层配置
<ul>
<li>默认配置</li>
<li><code>config.toml</code> 配置</li>
<li>环境变量配置</li>
</ul>
</li>
</ul>
<p>不过目前还有一些尚未确定是否应该是现有的实现方式，比如：</p>
<ul>
<li>有些配置是否应固定在 <code>theme</code> 里还是在 <code>config.toml</code> 里面设置</li>
<li>有些博客的信息是否应该放到每个页面模板 context 环境中</li>
</ul>
<p>这些就在以后使用和反馈中在逐渐修改吧。</p>
<p>那么现在就来看一下怎么用 <a href="https://github.com/FuGangqiang/mdblog.rs">mdblog.rs</a> 来构建博客吧！</p>
<h2>安装 rustc &amp; cargo</h2>
<p>因为 <code>mdblog.rs</code> 是由 rust 语言编写的，所以系统中需要安装 rustc 和 cargo，
推荐利用 <a href="https://rustup.rs/">rustup.rs</a> 来安装它们，目前我只在 nightly 版本测试编译 <code>mdblog.rs</code>：</p>
<pre><code>rustup default nightly
</code></pre>
<h2>安装 mdblog.rs</h2>
<pre><code>cargo install mdblog
</code></pre>
<h2>初始化博客文件夹</h2>
<pre><code>mdblog init myblog
</code></pre>
<p>此时博客目录树如下：</p>
<pre><code>myblog
├── config.toml
├── media
├── posts
│   ├── hello.md
│   └── math.md
└── _themes
    └── simple
</code></pre>
<p>其中：</p>
<ul>
<li><code>config.toml</code>：博客配置文件</li>
<li><code>media</code>：博客媒体文件夹</li>
<li><code>posts</code>：博客 markdown 文件夹
<ul>
<li><code>hello.md</code>：测试 markdown 文件</li>
<li><code>math.md</code>：测试 TeX math 文件</li>
</ul>
</li>
<li><code>_themes</code>：博客主题文件夹
<ul>
<li><code>simple</code>：博客预置 simple 主题</li>
</ul>
</li>
</ul>
<h2>编写博客文章</h2>
<pre><code>cd myblog
mdblog new another
</code></pre>
<p>运行 <code>new</code> 命令会创建一个 <code>posts/another.md</code> 的博客样例文件：</p>
<pre><code>created: 2018-04-09T11:15:22+08:00
tags: [blog]

this is a new post!
</code></pre>
<p>从上面可以看到，一篇博客分为两部分：</p>
<ul>
<li><code>header</code>：博客元信息，yaml 格式</li>
<li><code>body</code>：博客 markdown 内容</li>
</ul>
<p><code>header</code> 和 <code>body</code> 由第一个空行分割开，其中 <code>header</code> 有以下几个配置项：</p>
<ul>
<li><code>created</code>：必填项，博客生成日期</li>
<li><code>tags</code>：选填项，博客标签，默认: <code>[]</code></li>
<li><code>hidden</code>：选填项，博客是否隐藏，默认：<code>false</code></li>
<li><code>description</code>：选填项，博客概述，默认：body 的第一段或前 100 个字符</li>
</ul>
<h2>测试博客</h2>
<p>对刚才新创建的 <code>posts/another.md</code> 文件进行一些修改，
就可以检测一下最终的博客产生页面，运行：</p>
<pre><code>mdblog serve
</code></pre>
<p>该命令会自动打开浏览器，并进入博客的索引页面，你就可以点击查看新的博客文章了。</p>
<p>当你再次去修改某篇博客文章时，<code>serve</code> 命令会自动检测到你对某些文件进行了修改，
然后重新构建博客静态文件，这样你在浏览器中刷新页面就可以看到这些更新了。</p>
<h2>构建博客</h2>
<p>当你用 <code>serve</code> 命令查看博客的测试结果已基本符合你的要求后，就可以正式的构建博客了：</p>
<pre><code>mdblog build
</code></pre>
<p>这样所有的博客静态文件就被写入到 <code>_build</code> 文件夹里了。</p>
<h2>配置博客</h2>
<p><a href="https://github.com/FuGangqiang/mdblog.rs">mdblog.rs</a> 提供了许多可供自定义配置的选项，
可以通过 <code>config.toml</code> 文件查看都是有哪些可配置项以及它们的默认值：</p>
<pre><code>site_url = &quot;&quot;
site_name = &quot;Mdblog&quot;
site_motto = &quot;Simple is Beautiful!&quot;
footer_note = &quot;Keep It Simple, Stupid!&quot;
media_dir = &quot;media&quot;
build_dir = &quot;_build&quot;
theme = &quot;simple&quot;
theme_root_dir = &quot;_themes&quot;
rebuild_interval = 2
posts_per_page = 20
</code></pre>
<p>其中 <code>media_dir</code>、<code>build_dir</code>、<code>theme_root_dir</code> 是目录路径设置，
可以利用 shell 环境变量来指定路径，比如：</p>
<ul>
<li><code>$HOME/blog/media</code></li>
<li><code>~/blog/build</code></li>
</ul>
<h2>自定义博客主题</h2>
<p>可以通过自定义博客主题来修改你的博客样式，首先，你需要创建一个博客主题：</p>
<pre><code>mdblog theme new mytheme
</code></pre>
<p>这样就在 <code>_themes</code> 文件夹下生成了和 <code>simple</code> 一样的名为 <code>mytheme</code> 的主题，
然后你就可以对这个主题进行各种各样的修改了，
不过修改博客主题需要你熟悉：</p>
<ul>
<li>html/js/css</li>
<li><a href="https://tera.netlify.com/docs/installation/">tera</a> 模板，类似于 python 中的 Jinja2</li>
</ul>
<p>修改后你就可以应用这个主题了：</p>
<pre><code>mdblog theme set mytheme
</code></pre>
<p>或者直接修改 <code>config.toml</code> 中的 <code>theme</code> 选项就可以了。</p>
<h2>CLI 帮助</h2>
<pre><code>$ mdblog -h
mdblog 0.12.0
FuGangqiang &lt;fu_gangqiang@qq.com&gt;
static site generator from markdown files

USAGE:
    mdblog &lt;SUBCOMMAND&gt;

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

SUBCOMMANDS:
    build    Build the blog static files
    help     Prints this message or the help of the given subcommand(s)
    init     Initialize the blog directory layout
    new      Create a blog post
    serve    Serve the blog, rebuild on change
    theme    Blog theme operations
</code></pre>
<p>对子命令也可以查看：</p>
<pre><code>$ mdblog theme -h
mdblog-theme 0.12.0
FuGangqiang &lt;fu_gangqiang@qq.com&gt;
Blog theme operations

USAGE:
    mdblog theme &lt;SUBCOMMAND&gt;

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

SUBCOMMANDS:
    delete    Delete a theme
    help      Prints this message or the help of the given subcommand(s)
    list      list blog themes
    new       Create a new theme
    set       Set blog use the theme
</code></pre>

        ]]>
    </content>
  </entry><entry>
    <id>https://fugangqiang.github.io/posts/python/python%E4%B9%8B%E7%BD%91%E7%BB%9C%E7%88%AC%E8%99%AB%E6%A1%86%E6%9E%B6scrapy.html</id>
    <title>python之网络爬虫框架scrapy</title>
    <updated>2017-02-25T18:19:40+08:00</updated>
    <published>2017-02-25T18:19:40+08:00</published>
    <link href="https://fugangqiang.github.io/posts/python/python%E4%B9%8B%E7%BD%91%E7%BB%9C%E7%88%AC%E8%99%AB%E6%A1%86%E6%9E%B6scrapy.html"/>
    <summary>最近自己写了一个爬虫程序，用 scrapy 来爬取网上的几十万首诗词， 非常方便，本文总结一下。...</summary>
    <content type="html" xml:lang="en" xml:base="https://fugangqiang.github.io">
        <![CDATA[
        <p>最近自己写了一个爬虫程序，用 scrapy 来爬取网上的几十万首诗词，
非常方便，本文总结一下。</p>
<h2>什么是网络爬虫？</h2>
<p>现在已经步入互联网时代，我们接触的大多数信息都可以通过互联网来获取，
最基本的就是通过浏览器，键入网页地址，浏览网站，获取网站信息。</p>
<p>用户可以自行手工上网筛选信息的，当然这样效率是异常的低，
而网络爬虫通过程序自动化来自动访问网页链接，
自动分析网页内容，自动筛选出需要的信息，并将产生的信息数据加以保存起来，
进而大大提高了获取信息的效率。</p>
<p>爬虫程序之所以可以做到自动化，
皆因互联网一切网页都是符合相关标准协议的，
只需按照标准协议来解析网页数据，我们就可以结构化网页数据，并提取相关信息。</p>
<p>由于爬虫程序的结构、功能大多类似，
通过爬虫框架无疑会将我们从爬虫实现的细节中解脱出来，
我们只需关注怎么筛选出需要的数据就可以了，
本文讲解爬虫框架中最常见的 scrapy。</p>
<h2>scrapy 框架</h2>
<h3>安装 scrapy：</h3>
<pre><code>pip install scrapy
</code></pre>
<h3>创建爬虫工程</h3>
<p>scrapy 框架与 python django web 框架理念相似，
提供了相似的命令行界面：</p>
<pre><code>scrapy startproject tutorial
</code></pre>
<p>这样 scrapy 就创建了 <code>tutorial</code> 爬虫工程文件目录，
里面初始化了爬虫所需的基本文件，
从中可以看出 scrapy 爬虫程序的结构：</p>
<pre><code>$ tree tutorial
tutorial
├── scrapy.cfg
└── tutorial
    ├── __init__.py
    ├── items.py
    ├── middlewares.py
    ├── pipelines.py
    ├── __pycache__
    ├── settings.py
    └── spiders
        ├── __init__.py
        └── __pycache__

4 directories, 7 files
</code></pre>
<p>而我们写的相关爬虫程序只需在 <code>spiders</code> 目录里添加相关代码就可以了，
其他文件都是 scrapy 为了更好的扩展而准备的，此时可以不必理这些文件。</p>
<h3>自定义爬虫程序</h3>
<p>创建 <code>tutorial/tuorial/spiders/quotes.py</code> 文件：</p>
<pre><code>import scrapy


class QuotesSpider(scrapy.Spider):
    name = &quot;quotes&quot;
    start_urls = [
        'http://quotes.toscrape.com/page/1/',
    ]

    def parse(self, response):
        for quote in response.css('div.quote'):
            yield {
                'text': quote.css('span.text::text').extract_first(),
                'author': quote.css('small.author::text').extract_first(),
                'tags': quote.css('div.tags a.tag::text').extract(),
            }
</code></pre>
<p>从上面代码可以看出，我们只需指定：</p>
<ul>
<li><code>name</code>: 爬虫对应的 name，用于唯一标记爬虫程序</li>
<li><code>start_urls</code>: 爬虫开始的第一个 url 地址</li>
<li><code>parse</code>: 抓取页面中需要的信息</li>
</ul>
<p>其他与爬虫相关的所有细节，scrapy 已经提供好了，我们只需写这些就可以正是获取信息了。</p>
<h3>运行爬虫程序</h3>
<p>命令须在爬虫工程目录运行：</p>
<pre><code>scrapy crawl quotes -o quotes.json
</code></pre>
<p>这样，所有结果就保存在 <code>quotes.json</code> 文件里了。</p>
<h3>调试爬虫</h3>
<p>为了更好的实时调试，查看代码作用，scrapy 提供了 <code>shell</code> 子命令：</p>
<pre><code>scrapy shell 'http://quotes.toscrape.com/page/1/'
</code></pre>
<p>这样就进入了一个命令交互界面，可以运行相关 python 代码实时验证代码。</p>
<h3>其他</h3>
<p>上面只是一个简单的介绍，scrapy 提供了更多的抽象：</p>
<ul>
<li>Item</li>
<li>ItemLoader</li>
<li>ItemPipeline</li>
<li>Middleware</li>
<li>Setting</li>
<li>Logging</li>
<li>...</li>
</ul>
<p>具体请看 <a href="https://doc.scrapy.org/en/latest/">scrapy 文档</a></p>

        ]]>
    </content>
  </entry>
</feed>
